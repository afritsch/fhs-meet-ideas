<% content_for :title, t(".title", :project => @project.title) %>
<% content_for :headline do %><%= image_tag "/images/headlines/edit.png" %> <%= t(".title", :project => @project.title) %><% end %>

<%= render "projects" %>

<%= link_to t("common.back"), projects_path %>