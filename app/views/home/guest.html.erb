<% content_for :title, t("home.title") %>

<%= render "intro" %>

<div id="teaserBox">
  <div><%= t("home.teaser") %></div>
  <a href="<%= url_for login_path %>" class="actionButton"><span><%= t("home.button_login") %></span></a>
</div>