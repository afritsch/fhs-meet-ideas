<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="content-type" />
  </head>
  <body>
    <p><%= t("followups.mailer.body.welcome", :name => @user.fullname) %></p>
    <p><%= t("followups.mailer.body.text_1", :project => @project.title) %></p>
    <p>
      <%= t("followups.mailer.body.text_2") %><br />
      <%= link_to project_path(@project, :only_path => false) %>
    </p>
    <p><%= t("followups.mailer.body.salutation") %></p>
    <p><%= t("followups.mailer.body.noreply") %></p>
  </body>
</html>