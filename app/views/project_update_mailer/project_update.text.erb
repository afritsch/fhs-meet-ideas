<%= t("followups.mailer.body.welcome", :name => @user.fullname) %>

<%= t("followups.mailer.body.text_1", :project => @project.title) %>

<%= t("followups.mailer.body.text_2") %><br />
<%= project_path(@project) %>

<%= t("followups.mailer.body.salutation") %>