<!DOCTYPE html>
<html lang="<%= t(".lang") %>">

<head>
  <meta charset="utf-8" />
  
  <title><%= yield :title %> - meet ideas - FH Salzburg</title>
  
  <meta name="description" lang="<%= t(".lang") %>" content="<%= t(".meta_description") %>" />
  <meta name="keywords" lang="<%= t(".lang") %>" content="<%= t(".meta_keywords") %>" />
  <meta name="robots" content="index, follow" />
  
  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "jquery.noty.css" %>
  <%= stylesheet_link_tag "noty_theme_default.css" %>
  <%= stylesheet_link_tag "http://fonts.googleapis.com/css?family=Yanone+Kaffeesatz" %>
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "jquery.noty.js" %>
  <%= csrf_meta_tags %>
  <% if flash[:notice] || flash[:error] %>
  <script type="text/javascript">
    $(document).ready(function(){
      var noty_id = noty({
        "text":"<% if flash[:notice] %><%= flash[:notice] %><% else %><%= flash[:error] %><% end %>",
        "layout":"topCenter",
        "type":"<% if flash[:notice] %>success<% else %>error<% end %>"
      });
    });
  </script>
  <% end %>
</head>

<body>
  <div id="outerWrapper">
    <div id="innerWrapper">
      <div id="logo">
        <a href="<%= url_for root_path %>"></a>
      </div>
      <div id="button_login">
        <%= link_to "", login_path %>
      </div>
      <div class="clear"></div>
      <div id="content">
        <% if(current_user) %>
          Ich bin eingeloggt! (<%= link_to "Logout", logout_path %>)
        <% else %>
          Ich bin NICHT eingeloggt!
        <% end %>
        <%= yield %>
      </div>
    </div>
    <div id="footer">
      <div id="footerContent">
        <div id="fhsLogo"><%= link_to "", "http://www.fh-salzburg.ac.at/" %></div>
        <div id="footerText"><%= t(".footerText_1") %><br /><%= t(".footerText_2") %></div>
        <div id="footerLinks"><%= link_to t(".contact"), pages_imprint_path %> | <%= link_to t(".imprint"), pages_imprint_path %></div>
      </div>
    </div>
  </div>
</body>

</html>
